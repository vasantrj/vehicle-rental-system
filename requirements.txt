# ═══════════════════════════════════════════════════════════════════
#  DriveEase — System Requirements
#  (PHP projects use composer.json for packages; this file documents
#   the server environment and third-party libraries for reviewers.)
# ═══════════════════════════════════════════════════════════════════

# ── Server Environment ──────────────────────────────────────────────
PHP          >= 8.0
MySQL        >= 5.7  (or MariaDB >= 10.4)
Apache       >= 2.4  (with mod_rewrite enabled)
OpenSSL      (for SMTP TLS / HTTPS)
cURL         (PHP extension — for Razorpay API calls)
mbstring     (PHP extension)

# ── PHP Libraries (managed via Composer) ────────────────────────────
razorpay/razorpay    ^2.9    # Payment gateway SDK
rmccue/requests      ^2.0    # HTTP client (dependency of Razorpay)

# ── Bundled Libraries (included in repo, no install needed) ─────────
PHPMailer            ^6.8    # Email via SMTP   (phpmailer/src/)
FPDF                 ^1.86   # PDF generation   (fpdf/)

# ── Frontend CDN Dependencies (no install — loaded from CDN) ────────
Bootstrap            5.3.2   # UI framework
Font Awesome         6.5.0   # Icons
Flatpickr            latest  # Date picker
Chart.js             latest  # Analytics charts
Razorpay Checkout    v1      # Payment UI modal

# ── Development / Local Setup ───────────────────────────────────────
XAMPP / WAMP / Laragon   (Windows)
MAMP                      (macOS)
Composer                  (for running `composer install` if needed)

# ── Deployment (Production) ─────────────────────────────────────────
InfinityFree / Hostinger / Railway / Render (see README for guide)
